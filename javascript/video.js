function start(){	var video = document.getElementById("video");	if (video.paused)	{		video.play();	}	else	{		video.pause();	}}function stumm(){	var video = document.getElementById("video");	if (video.muted)	{		video.muted = false;	}	else	{		video.muted = true;	}}function lauter(){	var video = document.getElementById("video");	if (video.volume < 1)	{		video.volume = video.volume + 0.2;	}}function leiser(){	var video = document.getElementById("video");	if (video.volume > 0)	{		video.volume = video.volume - 0.2;	}}function schneller(){	var video = document.getElementById("video");	video.playbackRate = video.playbackRate + 0.2;}function langsamer(){	var video = document.getElementById("video");	video.playbackRate = video.playbackRate - 0.2;}function springeZu(){	var video = document.getElementById("video");	var zeit = document.getElementById("zeit");	if (zeit)	{		video.currentTime = zeit;	}}function standard(){	var video = document.getElementById("video");	video.playbackRate = 1;	video.volume = 1;	video.muted = false;	video.currentTime = 0;}function loadVideo(){	var video = document.getElementById("video");	var quelle = document.getElementById("auswahl").value;	video.load(quelle);}